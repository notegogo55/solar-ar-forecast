# Track A — data + imaging
sunpy[net]>=5.1       # includes Fido, GOES client, HEK
drms>=0.7.1           # JSOC/HMI/AIA export
astropy>=5.3
xarray>=2023.1        # GOES NetCDF4 parsing
netcdf4               # xarray NetCDF4 backend
scikit-image
# Track B — forecasting + eval
numpy
pandas
scikit-learn
scipy
pyyaml
tqdm
matplotlib
statsmodels
# PyTorch — LSTM (Track B local training on CPU/1650; preinstalled on Kaggle GPU images)
torch
torchvision
